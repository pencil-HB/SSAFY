<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- mapper file: 실행시킬 query를 가지고 있는 파일 -->

<mapper namespace="com.mvc.mapper.ProductMapper">
	<select id="selectAll" resultType="Product"> select * from product
	</select>
	
	<!--
	<select id="selectOne" parameterType="string" resultType="Product"> select
		* from product where num = #{xxx}
	</select>
	<update id="countUp" parameterType="String"> update product set count =
		count+1 where num = #{num}
	</update>
	<delete id="delete" parameterType="string"> delete from product where num =
		#{num}
	</delete>
	<update id="modify" parameterType="Product"> update product set title=
		#{title}, pass = #{pass}, content = #{content} where num = #{num}
	</update>
	<insert id="insert" parameterType="Product">
		insert into product(pass, name, title, wdate, content, count)
		values(#{pass},#{name},#{title}, now() ,#{content}, #{count})
	</insert>
	
	<select id="search" parameterType="map" resultType="Product">
		select * from product
		<if test='type eq "name"'>
			where name = word order by num desc
		</if>
		<if test='type eq "title"'>
			where title like #{word} order by num desc
		</if>
	</select> -->
</mapper>
